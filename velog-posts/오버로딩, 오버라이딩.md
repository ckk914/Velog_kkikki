<blockquote>
</blockquote>
<p>오버로딩: 한 클래스 내에서 같은 이름의 함수지만
파라미터의 개수나 타입이 '다른' 
여러 함수를 정의하는 것(함수명만 같고 파라미터만 다름).</p>
<blockquote>
</blockquote>
<p>오버라이딩: 상속 관계에서
부모 클래스의 메서드와 완전히 동일한 시그니처(함수명, 파라미터, 반환값)를 
자식 클래스에서 재정의하는 것입니다.</p>
<p>:&gt;</p>